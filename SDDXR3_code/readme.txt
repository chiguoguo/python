使用 modelR 来提取特征（提取训练集+测试集，提取预测集）

model_GBRT  生成一层模型 
model_RF    生成二层模型
model_Adaboost  #不用，不好

merge_model_lr.py  生成二层模型

mk_predict  应用到预测集


splitValiddata.py  切分数据（如果太大，或者将预测集切分为训练集）
phone_dict.pickle  沿用品牌价位模型的品牌价位字典
merge_output.py    合并输出（输入需要切割，因为加载不到内存）
choose_model.py    一些简单模型的尝试



第一层模型是使用很多GBRT和RF 来分别进行预测，预测完之后生成了各自的结果，即1和0.1表示购买。
label是未来2个月是否会更换手机，1是更换（购买）。
features是过去20个月的每个月是否购买了手机（20维度），过去20个月每个月购买了几部手机（20维度），过去20个月每个月及该月之前的那些月是否购买了手机（20维度），
过去20个月每个月及该月之前的那些月购买了几部手机（20维度），过去20个月每个月所使用的手机的价位（用数字表示，20个维度），过去二十个月每个月使用的手机品牌（针对品牌
做了oneHotEncoding，有26种品牌，所以每个月的品牌的维度从1维转换为26维，总共有
20个月，所以有520个维度）


问题：
第一层模型中的各个相同的模型参数不一样吧？怎样确定的各个相同模型的参数呢？
凭感觉规定的参数。本项目中是两个树的模型，规定树模型中的tree的数量不相同。